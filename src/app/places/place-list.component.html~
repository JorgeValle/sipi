<!-- renders main navigation, one of two versions -->
<main-menu-b></main-menu-b>

<filter-places-form></filter-places-form>

<div class="container">
  <div class="row mt-3 mb-3">
    <div class="col-10">
      <h1 class="h3">Los 10 mejores restaurantes en Antigua</h1>
    </div>
  </div>

  <div class="row">
    <div class="col-12 col-sm-12 col-md-8">
      <div class="card bg-light mb-4" *ngFor="let place of places">  
	<!--
        <div class="card-header">
          <i class="material-icons">grade</i>
	  <i class="material-icons">grade</i>
	  <i class="material-icons">grade</i>
	</div>
	-->
        <div class="card-body">
          <h2 class="card-title h4">
            <a routerLink="/lugar/{{place.id}}">{{place.name}}</a>
          </h2>
	  <!--
	  <i class="material-icons">view_comfy</i>
	  -->
	  <span class="card-subtitle mb-2 text-muted" *ngFor="let subcategory of place.subcategories">
	    <a routerLink="#">{{subcategory}}</a>,&nbsp;
	  </span>
	  <address class="card-text">
	    {{place.address}} <br>
	    {{place.city}}, {{place.country}} <br>
	    <i>{{place.phone}}</i>
	  </address>
	  <!--
	  <small class="text-muted">18 personas blah blah</small>
	  -->
	</div>
	<!--
        <div class="card-footer text-muted">
          2 days ago
        </div>
	-->
      </div><!-- /.card -->

      <pagination-menu></pagination-menu>

    </div>


    <div class="col-12 col-sm-12 col-md-4">
      <div id="map"></div>
    </div>


  </div>

</div>
